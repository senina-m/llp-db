#include "test/tests.h"

int main(){

    // const char *str = mmalloc_array(char, 15); // 14+\0
    // snprintf((char *)str, 15, "Hello world!!!");
    // Value_c c2 = {.string_value = str};
    // Value v2 = {.c = c2, .t = string_type};

    // Node *node2 = mmalloc(Node);

    // node2->offset = 0;
    // node2->first_child = 0;
    // node2->next_sibling = 0;
    // node2->node_len = 0;
    // node2->parent = 0;
    // node2->prev_sibling = 0;
    // node2->value_len = 0;
    // node2->value_t = 1;
    // node2->value_c = c2;

    // free_node(node2);


    // test1();
    // test2();
    // test3();
    test4();
    return 0;
}